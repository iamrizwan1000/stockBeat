name: Payouts
description: |-

  Real payout events (Plan §4.14) — what actually landed in the seller's
  bank account, distinct from order revenue. Read-only end to end: no
  create/update/delete, this is purely a reconciliation view. Shopify only
  in v1 — other platforms simply have no rows here yet, not an error.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/payouts
    metadata:
      custom: []
      groupName: Payouts
      groupDescription: |-

        Real payout events (Plan §4.14) — what actually landed in the seller's
        bank account, distinct from order revenue. Read-only end to end: no
        create/update/delete, this is purely a reconciliation view. Shopify only
        in v1 — other platforms simply have no rows here yet, not an error.
      subgroup: ''
      subgroupDescription: ''
      title: "List this team's polled payouts, newest first."
      description: |-
        Requires the plan's `analytics_level` to be `full` (Pro+, Plan §4.14/§5) —
        this was specified from the start but not actually enforced until this fix.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 200
        content: |-
          {
            "success": true,
            "message": null,
            "data": { "payouts": [
              { "id": 1, "connection_id": 1, "amount": 482.19, "currency": "USD", "status": "paid", "arrival_date": "2026-07-24" }
            ] }
          }
        headers: []
        description: success
      -
        custom: []
        status: 403
        content: |-
          {
            "success": false,
            "message": "Payouts requires the Pro plan or higher.",
            "errors": null
          }
        headers: []
        description: 'plan does not include payouts'
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
